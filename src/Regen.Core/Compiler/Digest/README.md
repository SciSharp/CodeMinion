#### Digest
Digest refers to an algorithm that accepts a template, cleanses-parses-compiles-evaluates-transforms-cleans it and finally and returning it.<br>
For a full Lexer and evaluator written from scratch, see `Regen.Compiler.Expressions` namespace.