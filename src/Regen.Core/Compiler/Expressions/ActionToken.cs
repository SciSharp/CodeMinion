namespace Regen.Compiler.Expressions {
    public enum ActionToken {
        Expression,
        Op,
        Declaration,
        Array,
        String,
        Boolean,
        Number,
        Emit,
        Foreach,
        EndOfLine,
    }
}